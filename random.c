#include <stdio.h>
#include <stdlib.h>
#include <time.h>


/* Function is used for running randomized characters 
   taking into account the 26 possible letter options
   made possible in the alphabet. These are then mulitplied
   and use modulus of that to select the valued letter. */
